import React from 'react';
import './style.css';

const Form = () => {
  return (
    <div className="form">
      <form>
        <h3>Let's stay in touch</h3>
        <input type="text" />
        <button type="submit">Send</button>
      </form>
    </div>
  );
};

export default Form;
